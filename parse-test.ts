// import { parseFunctionSignature } from "./lib/InputGenerator";

// /**
//  * Adds two numbers together.
//  * @param a The first number.
//  * @param b The second number.
//  */
// function add(a: number, b: number): number {
//   return a + b;
// }

// const functionInfo = parseFunctionSignature(add);

// const myFunc = (name: string, age: number): string => {
//   return `My name is ${name} and I am ${age} years old.`;
// };

// /**
//  * This is a function that greets a person.
//  * @param name The name of the person.
//  * @param age The age of the person.
//  */
// const myFunc2 = (name: string, age: number): string => {
//   return `My name is ${name} and I am ${age} years old.`;
// };

// const functionInfo2 = parseFunctionSignature(myFunc);
// console.log(functionInfo2);

// const functionInfo3 = parseFunctionSignature(myFunc2);
// console.log(functionInfo3);
